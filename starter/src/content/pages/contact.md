---
title: "Contact"
seo:
  title: "Contact"
  description: "Get in touch — send us a message."
blocks:
  - type: contactForm
    heading: Get in touch
    intro: Tell us what you need — we usually reply within one business day.
    # Booleans are seeded explicitly: the CMS shows an absent boolean as OFF even when the
    # site renders it ON (Decap applies `default:` to new entries only).
    showPhone: true
    showDirectContact: true
---
