name: Upgrade check
on:
  workflow_call:
    secrets:
      the control plane_hook_secret:
        required: false
jobs:
  check:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - run: echo "minimal + permissions"
