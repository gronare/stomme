name: Upgrade check
on:
  workflow_call:
    secrets:
      the caller_hook_secret:
        required: false
jobs:
  check:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - run: echo "minimal + permissions"
