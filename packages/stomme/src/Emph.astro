---
// `*words*` becomes an <em>, which styles.css renders as an underline, not italics.
interface Props { text?: string }
const parts = String(Astro.props.text ?? '').split(/\*([^*]+)\*/);
---
{parts.map((p, i) => (i % 2 ? <em>{p}</em> : p))}
