---
// Fallback target for the @stomme/slot-* aliases when no slot file is supplied (or
// STOMME_SLOTS_DIR is unset): renders nothing, so a site without slots builds exactly
// as before. See STOMME_SLOTS_DIR in integration.mjs.
---
